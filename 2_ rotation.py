# Here we build the rotation of the box ¨

# Input: 
    # Conceptual model (Box image of our domain, geometry with a added tunnel.)
    # Fracture network DFN (from Erik) 

# What is happening 
    # 15 degrees rotation, every rotation stores kij values.

# Output:
    # Conductivity tensor for each rotation, stored in a csv file. 